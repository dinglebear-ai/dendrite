# Changelog

## Unreleased

- Renamed the coding-session logger from `save-to-md` to `log-code-session`.
- Removed repository maintenance, tracker mutation, branch cleanup, commit, push, PR, merge, and HTML-rendering responsibilities.
- Added central knowledge-base output support and fleet-wide repository metadata.

Historical changes before this refactor were recorded under `save-to-md`.
