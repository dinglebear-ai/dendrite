# log-homelab-maintenance

Creates a curated maintenance record for live homelab operations using the canonical template under `~/docs/maintenance`.

It covers deployments, migrations, upgrades, incidents, repairs, audits, hardening, and routine work. The skill writes the record only. It does not change infrastructure or publish the artifact.
