# wrap-session

Routes a complete session to `log-code-session`, `log-homelab-maintenance`, both, or neither based on observed work.

It cross-links paired artifacts, refreshes knowledge indexes, and runs the docs validator. It does not publish artifacts or perform repository and infrastructure cleanup.
