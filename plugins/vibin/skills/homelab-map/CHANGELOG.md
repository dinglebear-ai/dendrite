# Changelog

## Unreleased

- Reframed homelab-map as a compiler and router over desired configuration, specialized docs inventories, and the compiled live map.
- Removed volatile container counts and version claims from the skill contract.
- Added `refresh-context.py` to orchestrate existing generators and emit a source manifest for drift reasoning.
