# save-to-md

Deprecated compatibility alias for `log-code-session`.

New workflows should use `log-code-session` for coding-only logging or `wrap-session` when the session may also require a homelab maintenance record.
